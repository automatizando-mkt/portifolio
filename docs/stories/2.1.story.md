# Story 2.1: Setup do Projeto Next.js e Configuração Base

## Status: Done

## Executor Assignment

executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, pattern_validation, build_verification]

## Story

**As a** Gestor de Automação com IA,
**I want** o projeto Next.js configurado com todas as ferramentas de desenvolvimento e os design tokens do Canvas Visual DS,
**so that** eu tenha uma base sólida e consistente para construir o portfólio.

## Acceptance Criteria

1. Projeto Next.js 16+ inicializado com App Router, TypeScript strict, Tailwind CSS, ESLint e Prettier
2. Estrutura de pastas segue o padrão da arquitetura: `app/`, `components/` (layout/, ui/, sections/, effects/), `lib/`, `content/cases/`, `public/cases/`, `test/`
3. Configuração de Tailwind com Canvas DS tokens: background `#050505`, accent `#F97316`, neutrals scale, tipografia Inter, animações fadeSlideIn
4. Script de dev (`npm run dev`) funciona e exibe página de canário ("Portfólio em construção") com estilo Canvas DS
5. `npm run lint` e `npm run typecheck` passam sem erros
6. Configuração SSG export no `next.config.ts` (`output: 'export'`, `images.unoptimized: true`)

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] **Task 1: Inicializar projeto Next.js** (AC: 1)
  - [x] Executar `npx create-next-app@latest portfolio --typescript --tailwind --app --eslint` dentro da raiz do projeto AIOS
  - [x] Configurar TypeScript strict mode em `tsconfig.json` (`strict: true`, `noUncheckedIndexedAccess: true`)
  - [x] Instalar Prettier e configurar `.prettierrc`
  - [x] Configurar path aliases: `@/*` → `./portfolio/*`

- [x] **Task 2: Criar estrutura de pastas** (AC: 2)
  - [x] Criar `components/layout/`, `components/ui/`, `components/sections/`, `components/effects/`
  - [x] Criar `lib/` com arquivo `utils.ts` (cn utility)
  - [x] Criar `content/cases/` (vazio, para Epic 3)
  - [x] Criar `public/cases/` (vazio, para imagens)
  - [x] Criar `test/` com setup Vitest básico

- [x] **Task 3: Configurar Tailwind com Canvas DS tokens** (AC: 3)
  - [x] Atualizar `tailwind.config.ts` com cores do Canvas DS:
    - Background: `#050505`
    - Surface: `#0A0A0A`
    - Accent: `#F97316`
    - Neutrals: usar escala padrão do Tailwind (já inclui neutral-300 a neutral-900)
  - [x] Adicionar font Inter via `next/font/google`
  - [x] Adicionar keyframes: `fadeSlideIn`, `beam-drop`, `beam-slide`
  - [x] Configurar selection colors: `selection:bg-[#F97316] selection:text-black`
  - [x] Configurar `globals.css` com body styles: `bg-[#050505] text-neutral-300 font-inter`

- [x] **Task 4: Configurar Next.js para SSG** (AC: 6)
  - [x] Atualizar `next.config.ts`: `output: 'export'`, `images: { unoptimized: true }`, `trailingSlash: true`

- [x] **Task 5: Criar página canário** (AC: 4)
  - [x] Criar `app/layout.tsx` com Inter font, globals.css import, meta tags básicos
  - [x] Criar `app/page.tsx` com: título "Portfólio em construção", subtítulo, visual usando Canvas DS tokens (bg dark, accent orange)
  - [x] Verificar que `npm run dev` exibe a página corretamente

- [x] **Task 6: Instalar dependências de desenvolvimento** (AC: 1, 5)
  - [x] Instalar: `lucide-react`, `clsx`, `tailwind-merge`
  - [x] Instalar dev: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `@vitejs/plugin-react`
  - [x] Criar `vitest.config.ts` com config básica
  - [x] Criar `lib/utils.ts` com `cn()` utility

- [x] **Task 7: Verificação final** (AC: 5)
  - [x] Executar `npm run lint` — deve passar sem erros
  - [x] Executar `npm run typecheck` — deve passar sem erros
  - [x] Executar `npm run build` — deve completar com sucesso (SSG export)

## Dev Notes

### Arquitetura de Referência

O projeto segue a arquitetura documentada em `docs/architecture/`:
- **Estrutura:** [Source: architecture/project-structure.md]
- **Tech stack:** Next.js 16+ App Router, TypeScript strict, Tailwind CSS 4, Lucide React [Source: architecture/tech-stack.md]
- **Design tokens:** Canvas Visual DS — dark theme #050505, accent #F97316, Inter font [Source: architecture/design-reference.md]

### Canvas DS — Tokens Críticos para Tailwind Config

```javascript
// tailwind.config.ts
{
  theme: {
    extend: {
      colors: {
        canvas: {
          bg: '#050505',
          surface: '#0A0A0A',
          accent: '#F97316',
        }
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', '-apple-system', 'sans-serif'],
      },
      keyframes: {
        fadeSlideIn: {
          '0%': { opacity: '0', transform: 'translateY(30px)', filter: 'blur(8px)' },
          '100%': { opacity: '1', transform: 'translateY(0)', filter: 'blur(0px)' },
        },
      },
      animation: {
        'fade-slide-in': 'fadeSlideIn 0.8s ease-out both',
      },
    }
  }
}
```

### Deploy Strategy

O projeto usa `output: 'export'` para gerar site estático compatível com Hostinger. Isso significa:
- **Sem API routes** (formulário de contato usará serviço externo no Epic 4)
- **Sem Next.js Image optimization** (usar `<img>` nativo com imagens otimizadas manualmente)
- **Todas as rotas pré-renderizadas** em build time
[Source: architecture/tech-stack.md#build--deploy-strategy]

### cn() Utility

```typescript
// lib/utils.ts
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```
[Source: architecture/coding-standards.md]

### Preset de Referência

O projeto segue o preset `nextjs-react` configurado no AIOS (`.aios-core/data/tech-presets/nextjs-react.md`). Para esta story (setup), os patterns do preset servem como guia de estrutura, não de lógica de negócio (portfólio é frontend-only).

### Testing

- **Framework:** Vitest + React Testing Library
- **Config:** `vitest.config.ts` com environment `jsdom`, setup file, path aliases
- **Coverage goals:** Utilities 90%+, Components 60%+, Overall 70%+
- **Test location:** `test/` directory ou co-locado com componentes
- **Para esta story:** Apenas setup do Vitest, sem testes de componentes ainda
[Source: architecture/testing-strategy.md]

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-02-18 | 1.0 | Story criada a partir do Epic 2 + Arquitetura | @aios-master (Orion) |
| 2026-02-18 | 1.1 | Validação PO: GO (9/10) — Status Draft → Ready | @po (Pax) |
| 2026-02-18 | 2.0 | Implementação completa — todas tasks e validações passam | @dev (Dex) |
| 2026-02-18 | 3.0 | QA Gate PASS — Status InReview → Done | @qa (Quinn) |

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6 (claude-opus-4-6)

### Debug Log References

Nenhum debug log necessário — implementação direta sem bloqueios.

### Completion Notes List

- Next.js 16.1.6 criado com create-next-app (Turbopack)
- Tailwind CSS 4 usa config via CSS `@theme` (não `tailwind.config.ts`) — tokens Canvas DS configurados em `globals.css`
- TypeScript strict + noUncheckedIndexedAccess habilitados
- SSG export configurado e build testado com sucesso
- Vitest 4.x instalado com jsdom environment
- `npm run lint`, `npm run typecheck` e `npm run build` passam sem erros

### File List

**Criados:**
- `portfolio/` — Projeto Next.js 16 completo
- `portfolio/app/layout.tsx` — Root layout com Inter font e meta tags
- `portfolio/app/page.tsx` — Página canário com Canvas DS tokens
- `portfolio/app/globals.css` — Tailwind 4 @theme com Canvas DS colors, keyframes, selection
- `portfolio/next.config.ts` — SSG export config
- `portfolio/tsconfig.json` — TypeScript strict + noUncheckedIndexedAccess
- `portfolio/.prettierrc` — Prettier config
- `portfolio/vitest.config.ts` — Vitest config com jsdom, path aliases
- `portfolio/lib/utils.ts` — cn() utility (clsx + tailwind-merge)
- `portfolio/test/setup.ts` — Vitest setup com jest-dom
- `portfolio/components/layout/.gitkeep`
- `portfolio/components/ui/.gitkeep`
- `portfolio/components/sections/.gitkeep`
- `portfolio/components/effects/.gitkeep`
- `portfolio/content/cases/.gitkeep`
- `portfolio/public/cases/.gitkeep`

**Modificados:**
- `portfolio/package.json` — Scripts typecheck, test adicionados + deps

---

## QA Results

### QA Gate Decision: PASS

**Reviewer:** @qa (Quinn) | **Date:** 2026-02-18 | **Model:** Claude Opus 4.6

### Acceptance Criteria Verification

| AC | Status | Evidence |
|---|---|---|
| AC1: Next.js 16+, TS strict, Tailwind, ESLint, Prettier | PASS | Next.js 16.1.6, strict + noUncheckedIndexedAccess, TW4, ESLint 9, Prettier 3.8 |
| AC2: Estrutura de pastas | PASS | 8/8 diretórios verificados |
| AC3: Tailwind Canvas DS tokens | PASS | @theme com canvas-bg, canvas-accent, Inter, fadeSlideIn |
| AC4: Dev server + página canário | PASS | Ready em ~1.3s, "Portfólio em construção" exibido |
| AC5: lint + typecheck | PASS | Zero erros em ambos |
| AC6: SSG export | PASS | output: 'export', unoptimized: true, trailingSlash: true |

### Independent Validation

- `npm run lint` — PASS (0 errors)
- `npm run typecheck` — PASS (0 errors)
- `npm run build` — PASS (SSG export, 4 pages, ~3s)
- Directory structure — PASS (8/8 directories exist)

### Observations (LOW — não bloqueiam)

1. Font variable `--font-inter` no layout não é referenciada no @theme (usa `--font-sans` diretamente) — funcional mas poderia ser mais coeso
2. `vitest.config.ts` usa `__dirname` — funciona hoje mas pode precisar ajuste futuro em ESM puro

### Verdict

**PASS** — Story 2.1 atende todos os 6 ACs. Código limpo, build funcional, sem riscos.
